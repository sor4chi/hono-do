{
  "$schema": "https://turbo.build/schema.json",
  "globalDependencies": ["**/.env.*local"],
  "pipeline": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**"]
    },
    "lint": {},
    "lint:check": {},
    "format": {},
    "format:check": {},
    "test": {
      "dependsOn": ["^build"]
    }
  }
}
